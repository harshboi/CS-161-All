#include <iostream>
#include <string>
#include <string.h>

using namespace std;

int main(){
   char h[10];
   string h[10];
   //getline(cin,h[0]); 
   cin.getline(h,6);  // C style string
   // cout << h[0];  // C style string
   cout << h[0][0]; // Possible with only C++ strings or declare 2-D C style String




}
